package dp_strategy;

// crear un intrface de base
public interface IEstrategia {
	
	// creamos el metotodo para aplicar nuestra estrategia
	void aplicarEstrategia();

}

// ahora solo creamos las clases que implementan este interface
// y cada una con un metodo implementado y asi
// y al futuro la extension es possible
